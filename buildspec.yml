version: 0.1

phases:
  build:
    commands:
      - sudo npm cache clean -f
      - sudo npm install -g n
      - sudo n stable
      - npm install
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - images/*
    - styles/*
    - dist/bundle.js
